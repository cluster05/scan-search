import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context';

import { StyleSheet, Text, View } from 'react-native'

const Filter = () => {
    return (
        <SafeAreaView>
            <Text>Filter Works</Text>
        </SafeAreaView>
    )
}

export default Filter

const styles = StyleSheet.create({})
